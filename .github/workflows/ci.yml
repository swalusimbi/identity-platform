name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: identity_platform_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Generate test signing keys
        run: |
          openssl genpkey -algorithm Ed25519 -out /tmp/jwt-private.pem
          openssl pkey -in /tmp/jwt-private.pem -pubout -out /tmp/jwt-public.pem

      - name: Write .env.test
        run: |
          {
            echo "PORT=5399"
            echo "NODE_ENV=development"
            echo "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/identity_platform_test"
            echo "REDIS_URL=redis://localhost:6379/15"
            echo "JWT_SECRET=ci-test-secret-at-least-32-characters-long"
            echo "JWT_PRIVATE_KEY=$(awk 'NF {printf "%s\\n", $0}' /tmp/jwt-private.pem)"
            echo "JWT_PUBLIC_KEY=$(awk 'NF {printf "%s\\n", $0}' /tmp/jwt-public.pem)"
            echo "JWT_KEY_ID=identity-platform-ci"
            echo "SERVICE_URL=http://localhost:5399"
            echo "ADMIN_KEY=ci-admin-key"
            echo "GOOGLE_CLIENT_ID=ci-google-client-id"
            echo "GOOGLE_CLIENT_SECRET=ci-google-client-secret"
            echo "MAIL_PROVIDER=memory"
          } > .env.test

      - name: Run integration tests
        run: npm test
